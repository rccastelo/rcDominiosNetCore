@model rcDominiosDataTransfers.PessoaTipoDataTransfer;

@{
    ViewData["Title"] = "";
}

<div class="areaMaxGrd">
    <div class="txtPadraoGrd"><i class="iconeMed fas fa-users"></i>&nbsp;
        <a class="txtPadraoGrd" asp-controller="PessoaTipo" asp-action="Index">Tipo de Pessoa</a> &nbsp;/&nbsp; Lista
    </div>
    <br>
    <table class="table border-bottom">
        <thead class="txtEtiqueta">
            <th>Id</th>
            <th>Descrição</th>
            <th>Código</th>
            <th>Ativo</th>
            <th></th>
        </thead>
        <tbody>
            @if (@Model.PessoaTipoLista != null) {
                @foreach (var item in @Model.PessoaTipoLista) {
                    <tr>
                        <td class="col-1"><a class="txtPadrao" href="/PessoaTipo/Form/@item.Id">@item.Id</a></td>
                        <td class="col-5"><a class="txtPadrao" href="/PessoaTipo/Form/@item.Id">@item.Descricao</a></td>
                        <td class="col-2"><a class="txtPadrao" href="/PessoaTipo/Form/@item.Id">@item.Codigo</a></td>
                        <td class="col-2"><a class="txtPadrao" href="/PessoaTipo/Form/@item.Id">@(item.Ativo ? "Sim" : "Não")</a></td>
                        <td class="col-2"><a class="btn btn-primary" href="/PessoaTipo/Form/@item.Id"><i class="fas fa-search-plus"></i> 
                            &nbsp;Detalhe</a></td>
                    </tr>
                }
            }
        </tbody>
    </table>
    <br>
    <div>
        <a href="/PessoaTipo"><button class="btn btn-secondary btn-lg mr-2"><i class="fas fa-arrow-circle-left"></i> 
                    &nbsp;Voltar</button></a>
    </div>
</div>
